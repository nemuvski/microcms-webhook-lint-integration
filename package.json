{
  "name": "microcms-webhook-lint-integration",
  "type": "module",
  "private": true,
  "scripts": {
    "lint": "biome lint ./",
    "lint:fix": "biome lint --apply ./",
    "format": "biome format ./",
    "format:fix": "biome format --write ./"
  },
  "workspaces": [
    "modules/*"
  ],
  "devDependencies": {
    "@biomejs/biome": "1.5.3",
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5.0.0"
  }
}
